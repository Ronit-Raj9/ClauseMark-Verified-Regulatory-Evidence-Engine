# Base URL of the rie-api FastAPI service. Leave blank in dev to use the
# Vite dev-server proxy (see vite.config.ts), or point at a deployed API.
VITE_API_BASE=
# Only used at vite-dev-server boot to proxy /v1 to the right backend.
VITE_API_PROXY=http://localhost:8080
