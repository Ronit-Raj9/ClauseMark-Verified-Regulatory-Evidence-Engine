# Implementation plan v2 — see `implementation.md` at repo root.

This file mirrors `../implementation.md` so that `docs/` browsing finds the canonical
reference. Treat the root file as the source of truth.
