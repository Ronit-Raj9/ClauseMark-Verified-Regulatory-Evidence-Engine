# Architecture v3 — see `systemArchitecture.md` at repo root.

This file mirrors `../systemArchitecture.md` so that `docs/` browsing still finds the
canonical reference. Treat the root file as the source of truth.
